# Dockerfile to run tests

FROM maven:3.8.5-jdk-11-slim

WORKDIR suiribot

COPY . .

ENTRYPOINT mvn install -DPOSTGRES_USER=${POSTGRES_USER} -DPOSTGRES_PASSWORD=${POSTGRES_PASSWORD} -DPOSTGRES_URL=${POSTGRES_URL} -DBOT_API_PATH=${BOT_API_PATH} -DBOT_TOKEN=${BOT_TOKEN}

